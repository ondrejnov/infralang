diff -ruN -x .local.changes -x __pycache__ -x .pytest_cache -x .ruff_cache examples/lvm/main.infra examples/lvm/main.infra
--- examples/lvm/main.infra	2026-08-18 00:12:19.570798807 +0200
+++ examples/lvm/main.infra	2026-08-18 00:15:37.786954737 +0200
@@ -4,13 +4,20 @@
 
 provider Lvm from "github.com/ondrejnov/lvm" version "0.5.0"
 
-# The provider manages logical volumes only. This volume group must already exist.
+# The provider manages logical volumes only. This volume group must already
+# exist on the remote host.
+input remoteHost: string = "10.0.0.2"
+input remoteUser: string = "root"
 input volumeGroup: string = "vg0"
 input volumeName: string = "infralang-data"
 input sizeGiB: number = 10
 
 configure lvm = Lvm({
   commandTimeoutSeconds: 120,
+  ssh: {
+    host: remoteHost,
+    user: remoteUser,
+  },
 })
 
 resource dataVolume = lvm.logicalVolume("data", {
diff -ruN -x .local.changes -x __pycache__ -x .pytest_cache -x .ruff_cache examples/lvm/README.md examples/lvm/README.md
--- examples/lvm/README.md	2026-08-18 00:11:55.986778478 +0200
+++ examples/lvm/README.md	2026-08-18 00:15:37.795954743 +0200
@@ -1,8 +1,8 @@
 # LVM example
 
 This example uses the local `github.com/ondrejnov/lvm` provider to create and
-grow a regular logical volume in an existing volume group. The provider does
-not create physical volumes, volume groups, filesystems, or mounts.
+grow a regular logical volume on `10.0.0.2` over SSH. The provider does not
+install LVM2 or create physical volumes, volume groups, filesystems, or mounts.
 
 Install version `0.5.0` of the provider into Terraform's local plugin mirror as
 described in `/var/www/terraform-provider-lvm/provider-lvm/README.md`, then run:
@@ -14,21 +14,23 @@
 terraform -chdir=examples/lvm plan
 ```
 
-The target volume group must already exist and the Terraform process needs
-permission to run LVM commands, usually as root. Increasing `sizeGiB` extends
+The target volume group must already exist on `10.0.0.2`. The default remote
+user is `root`; authentication and host-key verification use the SSH agent,
+`~/.ssh/config`, and standard known-hosts files. Increasing `sizeGiB` extends
 the logical volume, but does not resize its filesystem. Shrinking is rejected.
 
 The example sets `preventDestroy: true` because replacing or destroying the
 resource permanently removes the block device. Remove that lifecycle setting
 only when deletion is intentional.
 
-For remote management, replace the provider configuration with an SSH block:
+To use explicit SSH files, extend the provider configuration:
 
 ```infra
 configure lvm = Lvm({
+  commandTimeoutSeconds: 120,
   ssh: {
-    host: "storage.example.com",
-    user: "root",
+    host: remoteHost,
+    user: remoteUser,
     identityFile: pathexpand("~/.ssh/id_ed25519"),
     knownHostsFile: pathexpand("~/.ssh/known_hosts"),
   },
diff -ruN -x .local.changes -x __pycache__ -x .pytest_cache -x .ruff_cache README.md README.md
--- README.md	2026-08-18 00:11:55.987778479 +0200
+++ README.md	2026-08-18 00:15:37.796954744 +0200
@@ -46,8 +46,8 @@
 compatibility fixture; new infrastructure should prefer `terraform_data`.
 
 `examples/lvm` demonstrates safe logical-volume management with the local
-`github.com/ondrejnov/lvm` provider. It creates or grows an LV in an existing
-volume group and exposes its device path, UUID, and allocated size.
+`github.com/ondrejnov/lvm` provider over SSH. It creates or grows an LV in an
+existing volume group and exposes its device path, UUID, and allocated size.
 
 Generated `*.tf.json` files are ignored by Git because they are compiler
 artifacts.
