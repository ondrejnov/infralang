import type { HostCallConfig, VmConfig } from "./modules/libvirt_host/types.infra"
import type { VmBase } from "./modules/libvirt_vm/types.infra"

type HostDefinition = object {
  gateway: string,
  remoteHost: string,
  vms: map<VmConfig>,
}

type VmDefaults = VmBase
